spring:
  application:
    name: base-project-api
  datasource:
    url: jdbc:h2:mem:baseproject;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: sa
    driver-class-name: org.h2.Driver
  docker:
    compose:
      enabled: false

app:
  version: 0.1.0-SNAPSHOT
