# Libraries

Use this folder for shared code as the monorepo grows.

- `libs/web/*`: Angular feature, UI, and data-access libraries
- `libs/shared/*`: framework-agnostic TypeScript contracts and utilities
- Additional backend modules should stay under Gradle subprojects when they need their own JVM lifecycle
