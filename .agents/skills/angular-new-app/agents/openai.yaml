interface:
  display_name: "Angular New App"
  short_description: "Create modern Angular apps with CLI and MCP best practices"
  brand_color: "#DD0031"
  default_prompt: "Use $angular-new-app to scaffold a new Angular app with recommended AI configuration."
dependencies:
  tools:
    - type: "mcp"
      value: "angular_cli"
      description: "Angular CLI MCP server"
policy:
  allow_implicit_invocation: true
