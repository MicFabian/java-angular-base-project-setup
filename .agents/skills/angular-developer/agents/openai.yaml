interface:
  display_name: "Angular Developer"
  short_description: "Official Angular skill with Angular CLI MCP dependency"
  brand_color: "#DD0031"
  default_prompt: "Use $angular-developer to implement this Angular change with current best practices."
dependencies:
  tools:
    - type: "mcp"
      value: "angular_cli"
      description: "Angular CLI MCP server"
policy:
  allow_implicit_invocation: true
